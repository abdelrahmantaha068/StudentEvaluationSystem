#StdentEvaluationSystem
Update subject validation to use lowercase comparison

Changed the Subjects property setter to compare input values to lowercase strings ("oop" and "linq") for consistent, case-insensitive validation.

##project Notes
